{
  "name": "timobuilds-xyz",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/generate-agent-files.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "generate:agent": "node scripts/generate-agent-files.mjs",
    "verify:agent": "npm run build && node scripts/verify-agent-files.mjs"
  },
  "dependencies": {
    "@vercel/functions": "^2.2.13",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.3",
    "vitest": "^3.2.4"
  }
}
